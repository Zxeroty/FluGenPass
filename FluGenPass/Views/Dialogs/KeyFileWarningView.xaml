<UserControl x:Class="FluGenPass.Views.Dialogs.KeyFileWarningView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="400">
    <StackPanel>
        <TextBlock Text="{DynamicResource DlgKeyFileWarningMsg}" 
                   TextWrapping="Wrap" 
                   Margin="0,0,0,12"
                   Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
        
        <CheckBox x:Name="Check1" 
                     Content="{DynamicResource DlgKeyFileCheck1}" 
                     Margin="0,0,0,8"
                     Checked="CheckBox_Changed" 
                     Unchecked="CheckBox_Changed" />
        
        <CheckBox x:Name="Check2" 
                     Content="{DynamicResource DlgKeyFileCheck2}" 
                     Margin="0,0,0,8"
                     Checked="CheckBox_Changed" 
                     Unchecked="CheckBox_Changed" />
        
        <CheckBox x:Name="Check3" 
                     Content="{DynamicResource DlgKeyFileCheck3}" 
                     Margin="0,0,0,8"
                     Checked="CheckBox_Changed" 
                     Unchecked="CheckBox_Changed" />
    </StackPanel>
</UserControl>
